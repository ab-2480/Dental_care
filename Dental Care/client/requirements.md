## Packages
date-fns | Date formatting for appointments
lucide-react | Icons for the UI
react-hook-form | Form handling
@hookform/resolvers | Zod resolvers for forms
framer-motion | Smooth animations for page transitions and interactions

## Notes
Authentication uses session cookies (credentials: "include")
Role-based routing required (patient vs doctor)
